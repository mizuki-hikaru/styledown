# Second Post

Nothing fancy here. This file exists so the blog directory listing has more than one entry.

You can also verify that `.md` files themselves are omitted from listings and that the generated links point at the corresponding `.html` output.
