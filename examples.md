.hero.center.shadow:
    # mdserver Presentation Demo

    Markdown with composable presentation classes.

.caption.center:
    A tiny syntax extension that feels halfway between markdown and a UI system.

.buttons.blue.center:
    [Example Button A](#example-a)
    [Example Button B](#example-b)
    [View the markdown](/index.md)

.note:
    Notes are useful for contextual information and side remarks.

.success.shadow:
    Deployment completed successfully and all health checks passed.

.warning.large:
    This warning is slightly larger for emphasis.

.error.compact:
    Compact error blocks work well for inline failures.

.quote.large:
    “Readable source code is a feature.”

```
<html>
    <body>
        Hello world.
    </body>
</html>
```

.columns:
    .card.shadow:
        ## Shadow Card

        Multiple classes can layer behavior cleanly.

        - `.card`
        - `.shadow`

    .card.round:
        ## Rounded Card

        Utility classes can stay very small and composable.

        - `.card`
        - `.round`

.grid:
    .card.shadow.round:
        ### Combined Styles

        Cards can combine many presentation helpers.

    .note.compact.small:
        ### Compact Note

        Small informational block.

    .success.center:
        ### Centered Success

        Nicely aligned status messaging.

    .warning.shadow.large:
        ### Emphasized Warning

        Larger text plus depth.

.columns:
    .stat.center.shadow:
        **12**

        Pages migrated

    .stat.center.shadow:
        **3**

        Remaining tasks

    .stat.center.shadow:
        **99.95%**

        Uptime

.card.borderless.muted:
    ## Borderless Card

    Utility classes can radically alter appearance
    without introducing new semantic block types.

.table-wrap:
    | Classes | Effect |
    |---|---|
    | `.shadow` | Adds elevation |
    | `.round` | Larger border radius |
    | `.compact` | Reduced padding |
    | `.center` | Centers text |
    | `.small` | Smaller typography |
    | `.large` | Larger typography |
    | `.muted` | Softer text color |
    | `.borderless` | Removes borders |

.columns:
    .card.shadow:
        ### Nested Blocks

        .note.compact:
            Nested compositions work naturally.

        .success.compact.small:
            Tiny success callout.

    .card.round:
        ### Presentation as Composition

        Instead of inventing dozens of special components,
        a few semantic blocks plus utility classes scale elegantly.

.quote.center.large:
    “Small primitives compose better than giant frameworks.”

.hero.center.shadow.round:
    # Final Thoughts

    Multiple class support transforms the system from
    a styled markdown renderer into a lightweight document UI framework.
